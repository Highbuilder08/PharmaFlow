apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc

provisioner: efs.csi.aws.com

parameters:
  provisioningMode: efs-ap
  fileSystemId: REPLACE-WITH-EFS-FILE-SYSTEM-ID
  directoryPerms: "750"
  uid: "999"
  gid: "999"

reclaimPolicy: Retain
volumeBindingMode: Immediate
